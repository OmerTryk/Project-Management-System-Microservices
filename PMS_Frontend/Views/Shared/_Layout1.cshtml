<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - Proje Yönetim Sistemi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --accent-color: #4cc9f0;
            --light-color: #f8f9fa;
            --dark-color: #2d3748;
            --success-color: #4caf50;
            --warning-color: #ff9800;
            --danger-color: #f44336;
        }

        body {
            background-color: #f0f4f8;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            display: flex;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }

        .sidebar {
            width: 280px;
            background: linear-gradient(180deg, var(--dark-color) 0%, #1a202c 100%);
            color: white;
            padding-top: 20px;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
            z-index: 1;
            overflow-y: auto;
            flex-shrink: 0;
            min-height: 100vh;
            position: relative;
        }

        .sidebar-header {
            padding: 0 20px 20px 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 15px;
        }

        .sidebar-logo {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 15px;
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            background-color: var(--primary-color);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 10px;
        }

        .sidebar a {
            color: rgba(255, 255, 255, 0.8);
            padding: 12px 20px;
            display: flex;
            align-items: center;
            text-decoration: none;
            border-radius: 0 30px 30px 0;
            margin: 5px 0;
        }

            .sidebar a i {
                margin-right: 10px;
                font-size: 18px;
                width: 25px;
                text-align: center;
            }

            .sidebar a:hover {
                background-color: rgba(67, 97, 238, 0.1);
                color: white;
            }

            .sidebar a.active {
                background-color: var(--primary-color);
                color: white;
                font-weight: 500;
            }

            .sidebar a span {
                display: inline-block;
                margin-left: 5px;
            }

        .content-wrapper {
            flex: 1;
            padding: 20px;
            min-height: 100vh;
            width: calc(100% - 280px);
            overflow-x: hidden;
        }

        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 3px 15px rgba(0, 0, 0, 0.05);
            margin-bottom: 20px;
        }

        .card-header {
            background-color: white;
            border-bottom: 1px solid #f0f0f0;
            padding: 15px 20px;
            font-weight: 600;
        }

        .card-body {
            padding: 20px;
        }

        .form-control, .form-select {
            border-radius: 8px;
            padding: 10px 15px;
            border: 1px solid #e0e0e0;
        }

            .form-control:focus, .form-select:focus {
                border-color: var(--primary-color);
                box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.25);
            }

        label.form-label {
            font-weight: 500;
            margin-bottom: 8px;
            color: #555;
        }

        .btn {
            border-radius: 8px;
            padding: 8px 16px;
            font-weight: 500;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

            .btn-primary:hover {
                background-color: var(--secondary-color);
                border-color: var(--secondary-color);
            }

        .btn-success {
            background-color: var(--success-color);
            border-color: var(--success-color);
        }

        .btn-danger {
            background-color: var(--danger-color);
            border-color: var(--danger-color);
        }

        .table {
            border-collapse: separate;
            border-spacing: 0;
            width: 100%;
        }

            .table th {
                background-color: #f8f9fa;
                color: #555;
                font-weight: 600;
                padding: 12px 15px;
                border-bottom: 2px solid #e9ecef;
            }

            .table td {
                padding: 12px 15px;
                border-bottom: 1px solid #f0f0f0;
                vertical-align: middle;
            }

            .table tbody tr:hover {
                background-color: #f8f9fa;
            }

        .message-container {
            max-height: 400px;
            overflow-y: auto;
            border: 1px solid #e9ecef;
            border-radius: 10px;
            padding: 15px;
            background-color: white;
        }

        .message-item {
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid #f0f0f0;
        }

            .message-item:last-child {
                border-bottom: none;
                margin-bottom: 0;
            }

        .message-sender {
            font-weight: 600;
            color: var(--dark-color);
        }

        .message-content {
            margin: 8px 0;
        }

        .message-time {
            color: #aaa;
            font-size: 0.85rem;
        }

        .page-heading {
            margin-bottom: 20px;
            color: var(--dark-color);
            font-weight: 600;
            border-bottom: 1px solid #e0e0e0;
            padding-bottom: 15px;
        }
    </style>
</head>
<body>
    <nav class="sidebar">
        <div class="sidebar-header">
            <div class="sidebar-logo">
                <div class="logo-icon">
                    <i class="fas fa-project-diagram text-white"></i>
                </div>
                <h5 class="mb-0 logo-text">Proje Yönetim</h5>
            </div>
            <div class="text-center">
                <small class="text-light opacity-75">Yönetim Paneli</small>
            </div>
        </div>

        <a href="/Home/Index" class="@(ViewData["Title"]?.ToString() == "Home Page" ? "active" : "")">
            <i class="fas fa-home"></i>
            <span>Dashboard</span>
        </a>
        <a href="/Project/Project" class="@(ViewData["Title"]?.ToString() == "Proje Oluştur" ? "active" : "")">
            <i class="fas fa-plus-circle"></i>
            <span>Proje Oluştur</span>
        </a>
        <a href="/Project/ProjectView" class="@(ViewData["Title"]?.ToString() == "Projelerim" ? "active" : "")">
            <i class="fas fa-list"></i>
            <span>Projeleri Görüntüle</span>
        </a>
        <a href="/Message/Message" class="@(ViewData["Title"]?.ToString() == "Message" ? "active" : "")">
            <i class="fas fa-envelope"></i>
            <span>Mesajlar</span>
        </a>
        <a href="/Task/CreateTask" class="@(ViewData["Title"]?.ToString() == "Görev Oluştur" ? "active" : "")">
            <i class="fas fa-plus-square"></i>
            <span>Görev Oluştur</span>
        </a>
        <a href="/Task/TaskList" class="@(ViewData["Title"]?.ToString() == "Görevlerim" ? "active" : "")">
            <i class="fas fa-tasks"></i>
            <span>Görevlerim</span>
        </a>
      @*   <a href="#">
            <i class="fas fa-users"></i>
            <span>Takım Üyeleri</span>
        </a>
        <a href="#">
            <i class="fas fa-chart-bar"></i>
            <span>Raporlar</span>
        </a>
        <a href="#" class="mt-auto">
            <i class="fas fa-cog"></i>
            <span>Ayarlar</span>
        </a> *@
        <a href="/User/Profile" class="@(ViewData["Title"]?.ToString() == "Profil Düzenleme" ? "active" : "")">
            <i class="fas fa-user-edit"></i>
            <span>Profil Düzenle</span>
        </a>
        <a href="/User/Login" class="mt-2 mb-3">
            <i class="fas fa-sign-out-alt"></i>
            <span>Çıkış Yap</span>
        </a>
    </nav>

    <div class="content-wrapper">
        <h3 class="page-heading">@(ViewData["ContentName"] ?? ViewData["Title"])</h3>
        @RenderBody()
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>